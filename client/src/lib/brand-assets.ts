/** Primary brand mark (`client/public/images/brand/split-mark.png`). */
export const brandMarkPublicPath = "/images/brand/split-mark.png" as const;

/**
 * Tab + home-screen icons (resized from the mark).
 * Paths must match `<link rel="icon" …>` / `apple-touch-icon` in `index.html`.
 */
export const brandFaviconPublicPath = "/images/brand/favicon-32.png" as const;
export const brandAppleTouchIconPublicPath = "/images/brand/apple-touch-icon.png" as const;
